node_modules
dist
.env
.env.local
.DS_Store
*.local
.agents
.claude
skills-lock.json

# Backend compiled output
backend/dist

# Frontend build output
frontend/dist

# Local file uploads (used as a fallback when R2 credentials are not set)
.uploads
