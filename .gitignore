node_modules
dist
.env
.env.local
.DS_Store
*.local
.agents
.claude
skills-lock.json

# Backend compiled output
backend/dist

# Frontend build output
frontend/dist

# Local file uploads — used by the backend whenever NODE_ENV !== 'production'
# (always), and as a fallback in production when R2 credentials are not set.
.uploads
